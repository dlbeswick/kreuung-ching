#!/bin/bash
# This is a prerequisite for generating signed APKs from fdroid output for testing on emulators.
keytool -genkey -v -keystore android.keystore -alias dev -keyalg RSA -keysize 2048 -validity 10000